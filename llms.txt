# BAIT Life Sustaining Therapy (BAITLiST)

Behaviourial artificial intelligence technology (BAIT) for continuation
or withdrawal of Life Sustaining Therapy (LST) in the Intensive Care
unit

## Setup R environment

Uses the `renv` package to create a reproducible environment

``` r
# Setup a renv environment
# renv::init()

# install individual packages
# install.packages("tidyverse")
# install.packages("apollo")
# install.packages("readxl")
# install.packages("svglite")
# install.packages("ggsignif")
# install.packages("gt")
# install.packages("pkgdown")
# install.packages("katex")
# install.packages("svglite")

# Update packages using renv
# renv::update

# Snapshot after changes
renv::snapshot()

# restore environment
renv::restore()
```

## Load development packages

Used for developing this package

``` r
# install.packages(c("devtools", "roxygen2", "testthat", "knitr"))
library(devtools)

# Add devtools to .Rprofile
# use_devtools()

# Check if correctly installed
devtools::dev_sitrep()

# Creates package
# usethis::create_package("baitlist")

# Add dependencies
# usethis::use_package("rlang")
# usethis::use_package("dplyr")
# usethis::use_package("tidyr")
# usethis::use_package("stringr")
# usethis::use_package("readxl")
# usethis::use_package("apollo")
# usethis::use_package("ggplot2")
# usethis::use_package("ggsignif")
# usethis::use_package("gt")
# usethis::use_package("magrittr")
# usethis::use_package("pkgdown")
# usethis::use_package("katex")
# usethis::use_package("svglite")

# Update documentation
devtools::document()

# Test package
devtools::test()

# Update documentation, builds and checks
devtools::check()
```

## Running analysis

Model the data and calculate feature importance across groups of
respondents

``` r
# Load baitlist package
# Allows accessing functions with baitlist::fun()
devtools::load_all()

# Fit binary logit models for all groups. Disables backward elimination by setting threshold to 1
baitlist::fit_bait_binary(elimination_threshold = 1)

# Fit multinomial logit models for all groups. Disables backward elimination by setting threshold to 1
baitlist::fit_bait_multinomial(elimination_threshold = 1)

# Calculate feature importance using standardized coefficients
baitlist::standardized_coefficients()

# Calculate feature importance using maximum utility contribution
baitlist::maximum_utility_contribution()

# Compare model weights between models using Wald test
baitlist::compare_models()

# Create tables and figures
baitlist::create_criteria_table()
baitlist::create_model_weights_tables()
baitlist::create_group_comparison_table()
baitlist::plot_relative_importance()
baitlist::plot_group_comparison()
```

## Package Documentation

Documentation of this package was created using
[roxygen2](https://roxygen2.r-lib.org/) and
[pkgdown](https://pkgdown.r-lib.org/). The most recent documentation can
be found [here](https://patrickthoral.github.io/baitlist/).

    # Run once to configure your package to use and deploy pkgdown
    usethis::use_pkgdown_github_pages()

    # Preview your site locally before publishing
    pkgdown::build_site()
    pkgdown::build_home_index()
    pkgdown::init_site()

# Package index

## All functions

- [`coefficient_to_variable()`](https://patrickthoral.github.io/baitlist/reference/coefficient_to_variable.md)
  : Converts apollo coefficient_name (b\_) to variable name.
- [`compare_models()`](https://patrickthoral.github.io/baitlist/reference/compare_models.md)
  : Compare feature importance of models using the Wald test
- [`create_criteria_table()`](https://patrickthoral.github.io/baitlist/reference/create_criteria_table.md)
  : Create Criteria table
- [`create_group_comparison_table()`](https://patrickthoral.github.io/baitlist/reference/create_group_comparison_table.md)
  : Create group comparison table
- [`create_model_weights_tables()`](https://patrickthoral.github.io/baitlist/reference/create_model_weights_tables.md)
  : Create Model Weights tables
- [`fit_bait_binary()`](https://patrickthoral.github.io/baitlist/reference/fit_bait_binary.md)
  : Fits the responses in a BAIT binary logistic regression model for
  each respondent group
- [`fit_bait_multinomial()`](https://patrickthoral.github.io/baitlist/reference/fit_bait_multinomial.md)
  : Fits the responses in a BAIT multinomial logistic regression model
  for each respondent group
- [`load_responses()`](https://patrickthoral.github.io/baitlist/reference/load_responses.md)
  : Load responses Reads data from the data/responses.xlsx file for
  specific groups or all if not specified.
- [`maximum_utility_contribution()`](https://patrickthoral.github.io/baitlist/reference/maximum_utility_contribution.md)
  : Calculate relative importance using maximum utility contribution.
- [`plot_group_comparison()`](https://patrickthoral.github.io/baitlist/reference/plot_group_comparison.md)
  : Plots group comparison of coefficient weights
- [`plot_relative_importance()`](https://patrickthoral.github.io/baitlist/reference/plot_relative_importance.md)
  : Plot Relative Importance
- [`predict()`](https://patrickthoral.github.io/baitlist/reference/predict.md)
  : Predict choice based on patient characteristics and family values
- [`standardized_coefficients()`](https://patrickthoral.github.io/baitlist/reference/standardized_coefficients.md)
  : Calculate feature importance using standardized coefficients

